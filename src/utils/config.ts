// This public path might change accodingly to environments. It is the path which will give access to everything inside /public folder.
// You should use it as so : <img src={PUBLIC_PATH + "/images/maSuperImg.png"} />
// In local environnement, public folder can be accessed with a simple "/images/maSuperImg.png", hence the current config "".
export const PUBLIC_PATH = "";
